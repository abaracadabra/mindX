<IfModule mod_ssl.c>
<VirtualHost *:443>
    ServerName prom.pythai.net
    ServerAdmin mastermind@pythai.net

    SSLEngine on
    SSLCertificateFile      /etc/letsencrypt/live/prom.pythai.net/fullchain.pem
    SSLCertificateKeyFile   /etc/letsencrypt/live/prom.pythai.net/privkey.pem
    Include /etc/letsencrypt/options-ssl-apache.conf

    Header always set Strict-Transport-Security "max-age=63072000; includeSubDomains"
    Header always set X-Content-Type-Options "nosniff"
    Header always set X-Frame-Options "DENY"
    Header always set Referrer-Policy "no-referrer"

    # Operator-only — Prometheus has no native auth
    <Location />
        AuthType Basic
        AuthName "mindX Prometheus (operator)"
        AuthUserFile /etc/apache2/htpasswd/prom.htpasswd
        Require valid-user
    </Location>

    ProxyPreserveHost On
    ProxyRequests Off
    ProxyPass        / http://127.0.0.1:9090/
    ProxyPassReverse / http://127.0.0.1:9090/

    ErrorLog  ${APACHE_LOG_DIR}/prom.pythai.net-error.log
    CustomLog ${APACHE_LOG_DIR}/prom.pythai.net-access.log combined
</VirtualHost>
</IfModule>

# Moved → `openagents/bankoneth/docs/`

The ENS / bankon.eth design docs that used to live here have been consolidated into the
bankoneth module:

- Architecture / ENS / registry design → [`../../bankoneth/docs/design/`](../../bankoneth/docs/design/)
  (`BANKON_ARCHITECTURE.md`, `BANKON_ENS.md`, `README.md`)
- Operational + spec docs → [`../../bankoneth/docs/`](../../bankoneth/docs/)
  (`ARCHITECTURE.md`, `DEPLOYMENT.md`, `ENSIP_COVERAGE.md`, `specs/`, …)

> `SUBNAME_REGISTRY.md` was byte-identical to `BANKON_ENS.md` and was dropped as a duplicate.

See [`../../bankoneth/docs/CONSOLIDATION.md`](../../bankoneth/docs/CONSOLIDATION.md) for the
full inventory of what moved and from where.

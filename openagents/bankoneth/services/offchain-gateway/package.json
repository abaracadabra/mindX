{
  "name": "@bankoneth/offchain-gateway",
  "version": "0.1.0",
  "description": "CCIP-Read (EIP-3668) gateway for the bankoneth off-chain subname registrar (Phase 2.2). Receives queries from BankonOffchainResolver, looks up records in its store, EIP-712 signs responses against an allowlisted signer set.",
  "license": "Apache-2.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "dev":   "bun run --watch src/index.ts",
    "start": "bun run src/index.ts",
    "build": "bun build src/index.ts --target=bun --outdir=dist"
  },
  "dependencies": {
    "hono": "^4.6.0",
    "viem": "^2.35.0"
  },
  "devDependencies": {
    "@types/bun":    "^1.1.0",
    "typescript":    "^5.4.0"
  }
}

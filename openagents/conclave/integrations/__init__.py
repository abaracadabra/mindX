"""Optional integration adapters for CONCLAVE consumers.

These modules are illustrative: CONCLAVE itself does not import any of
them. Each adapter shows how a specific agent framework can compose the
protocol. Other frameworks ship their own adapter the same way.
"""

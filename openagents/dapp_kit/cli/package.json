{
  "name": "@openagents/dapp-kit-cli",
  "version": "0.1.0",
  "description": "openagents-dapp CLI — scaffold + dev + build + deploy.",
  "license": "Apache-2.0",
  "type": "module",
  "bin": {
    "openagents-dapp": "./bin/openagents-dapp.js"
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist",
    "bin",
    "src"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json && chmod +x bin/openagents-dapp.js",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@openagents/deploy": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}

# Apache reverse-proxy fragment for dojo-service.
# Same shape as boardroom-svc.conf — three transports with proper tuning.

RewriteEngine On
RewriteCond %{HTTP:Upgrade}     =websocket [NC]
RewriteCond %{HTTP:Connection}  upgrade    [NC]
RewriteRule ^/dojo-svc/(.*)$    ws://127.0.0.1:8772/$1  [P,L]

<Location /dojo-svc/>
    ProxyPass        http://127.0.0.1:8772/  flushpackets=on connectiontimeout=10 timeout=600
    ProxyPassReverse http://127.0.0.1:8772/

    Header always set Cache-Control "no-cache, no-store, must-revalidate"
    Header always set Pragma         "no-cache"
    Header always set Access-Control-Allow-Origin  "https://mindx.pythai.net"
    Header always set Access-Control-Allow-Methods "GET, POST, OPTIONS"
    Header always set Access-Control-Allow-Headers "Authorization, Content-Type"
    Header always set Access-Control-Allow-Credentials "true"
</Location>

SetEnvIfNoCase Request_URI ^/dojo-svc/ no-gzip dont-vary

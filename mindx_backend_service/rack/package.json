{
  "name": "@mindx/substrate-rack",
  "private": true,
  "version": "1.0.0",
  "description": "Read-only dreamknob instrument rack for the landing page — renders /insight/substrate/evolution as studio hardware. Build output is committed at ../static/substrate_rack.js; npm install + npm run build only when changing the rack.",
  "type": "module",
  "scripts": {
    "build": "node build.mjs"
  },
  "dependencies": {
    "dreamknob": "file:vendor/dreamknob-1.1.4.tgz",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
